export interface canvasOptions {
    selector            : string;
    width               : number
    height              : number;
    canvasBackground    : string;
    paperBackground     : string;
}